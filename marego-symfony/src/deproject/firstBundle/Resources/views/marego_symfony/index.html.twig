{% extends '::base.html.twig' %}

{% block body -%}
    <h1>marego_symfony list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Partner</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('marego_symfony_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.partner }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('marego_symfony_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('marego_symfony_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('marego_symfony_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
